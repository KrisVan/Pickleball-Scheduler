import { render, screen } from '@testing-library/react';
import App from './App';

/* Test Example
test('renders learn react link', () => {
  render(<App />);
  const linkElement = screen.getByText('Cities');
  expect(linkElement).toBeInTheDocument();
});
*/